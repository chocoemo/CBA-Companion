export default function PowerSpeedPage() {
  return (
    <p style={{ opacity: 0.7 }}>
      Power/Speed view (à la Choco's Dashboard) — coming with the custom
      at-least/at-most filter builder and Choco's exact preset parameters.
    </p>
  );
}
