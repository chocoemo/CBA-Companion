export default function TeamPage() {
  return (
    <p style={{ opacity: 0.7 }}>
      Your org's roster, depth chart, and sim-over-sim trend recommendations —
      scoped for a coming phase (needs game-log/box-score history to detect trends first).
    </p>
  );
}
